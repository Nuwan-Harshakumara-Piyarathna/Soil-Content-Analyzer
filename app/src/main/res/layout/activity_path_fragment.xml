<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/activity_twitter_login_background"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="16dp"
    android:weightSum="7">

    <Button
        android:id="@+id/btn_bluetooth"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_weight="1"
        android:background="@color/primaryColor"
        android:text="@string/fragment_steps_start_button_text"
        android:textColor="@color/primaryTextColor" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Walk through Your field and help us to map it"
        android:textColor="@color/primaryTextColor"
        android:textSize="25dp"
        android:layout_weight="4"/>
    <!-- twitter login button to do login -->
    <Button
        android:id="@+id/btn_start_walk"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_weight="1"
        android:text="Start"
        android:textSize="20dp"
        />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Press the start button when you start walking"
        android:textColor="@color/primaryTextColor"
        android:textSize="15dp"
        android:layout_weight="1"/>

</LinearLayout>